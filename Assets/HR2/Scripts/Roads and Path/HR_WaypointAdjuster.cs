using UnityEngine;

/// <summary>
/// A script manually added to specific waypoints.
/// If this script is present, the lanes will also be adjusted along the Z-axis.
/// </summary>
public class HR_WaypointAdjuster : MonoBehaviour
{
    public bool adjustZAxis = true;
}
